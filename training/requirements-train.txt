# Heavy training dependencies — install on GPU/CPU training machine / Colab only.
# Not required for baseline training, evaluation, or dataset validation.
#
# Teacher: PyTorch + transformers (TF backend removed in transformers 5+)
# Student distill/prune/quantize: TensorFlow + tensorflow-model-optimization

torch>=2.1.0
transformers>=4.40.0
tensorflow>=2.16.0
tensorflow-model-optimization>=0.8.0
scikit-learn>=1.3.2
PyYAML>=6.0
numpy>=1.24.0

# Teacher weights: provide --model-path to a local bert-base-multilingual-cased
# cache. Prefer intranet mirror; mark Hugging Face downloads as third-party.
